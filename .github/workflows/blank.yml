name: CI

on:
  workflow_dispatch:
    inputs:
      name:
        description: 'A name to be greeted with'
        default: 'world'
        required: false
        type: string

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run: echo Hello ${{ inputs.name }}!
